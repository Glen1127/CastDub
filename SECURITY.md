# Security and privacy

Do not include licensed media, voice data, model credentials, access tokens, or
private project manifests in a public report.

Before a public repository is connected, report issues directly to the project
owner. After publication, use the repository's private vulnerability reporting
channel for security-sensitive findings.

The application is local-first and must not transmit project content unless the
operator explicitly configures and approves a remote provider.
