# Private Series Asset Library

This directory is the cumulative, private voice and identity library for authorised series production. Generated contents are excluded from Git.

Each character has a stable `character_id`. New episode audio is stored as a candidate reference under that character and never replaces the selected long-term voice without approval. Original filenames remain recorded as provenance only; they are not treated as character identity.
